
<div class="row">
    <div class="col-sm-3 col-md-2 list-group">
        {{#link-to "otp.validate"  class="list-group-item"}}Validate OTP{{/link-to}}

        {{#if session.data.authenticated.jwt.needs_2f}}
            {{#link-to "otp.deactivate" class="list-group-item"}}Deactivate OTP{{/link-to}}
        {{else}}
            {{#link-to "otp.activate" class="list-group-item"}}Activate OTP{{/link-to}}
        {{/if}}

    </div>
    <div class="col-sm-9 col-md-9 sidebar">
        {{outlet}}
    </div>
</div>